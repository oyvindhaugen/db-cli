User: oyvind
Password: iktfag
Database: test_db

CREATE TABLE IF NOT EXISTS shopping (id SERIAL PRIMARY KEY NOT NULL, item VARCHAR NOT NULL, amount INT NOT NULL, owner INT NOT NULL);
CREATE TABLE IF NOT EXISTS users (id SERIAL PRIMARY KEY NOT NULL, username VARCHAR UNIQUE NOT NULL, password VARCHAR NOT NULL);

Clone db-cli origin/showcase 